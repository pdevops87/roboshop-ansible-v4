- name: copy user local to remote
  ansible.builtin.template:
    src: "{{ component }}.service"
    dest: /etc/systemd/system/{{ component }}.service

- name: Use role in loop
  ansible.builtin.include_role:
    name: common
    tasks_from: "{{ item }}"
  loop:
    - node
    - main
    - npm
    - backend_script
  loop_control:
    loop_var: item